; Explicit request-size limits, since all real file uploads (avatars,
; documents) go straight from the browser to Uploadcare rather than through
; this app -- PHP only ever needs to accept JSON/form bodies here.
post_max_size = 8M
upload_max_filesize = 2M
max_input_time = 60
