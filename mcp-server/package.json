{
  "name": "launchhr-mcp-server",
  "version": "1.0.0",
  "private": true,
  "description": "MCP server exposing LaunchHR's AI tools (time off, absence reporting) to Claude Desktop, Claude Code, and other MCP clients.",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
